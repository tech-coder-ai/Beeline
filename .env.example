# Copy to .env and fill in for docker-compose (compose auto-loads .env).

# OpenAI-compatible provider (used when llm.active = openai in settings.yaml)
OPENAI_API_KEY=

# Stellar provider: your local endpoint accepting {"systemPrompt": "...", "userMessage": "..."}
STELLAR_ENDPOINT=http://host.docker.internal:9000/generate
